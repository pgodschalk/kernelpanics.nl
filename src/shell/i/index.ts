export default function make(args: string) {
  if (args === "read the source code") {
    // i read the source code
    return '<span class="sr-only">ASCII representation of a heart</span><span aria-hidden="true"><3</span>'
  } else {
    // i
    return "bash: i: command not found"
  }
}
