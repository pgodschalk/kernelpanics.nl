export default function hello(args: string) {
  if (args === "joshua") {
    // hello joshua
    return "How about a nice game of Global Thermonuclear War?"
  } else {
    // hello
    return "Hello."
  }
}
